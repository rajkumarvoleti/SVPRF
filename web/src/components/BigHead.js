import React from "react";
import Button from "react-bootstrap/Button";

class BigHead extends React.Component {
  render() {
    return (
      <div className="head">
        <h1>Sri Padha Research Organisation</h1>
        <Button class="btn" size="lg">
          Contact Us
        </Button>
      </div>
    );
  }
}

export default BigHead;
